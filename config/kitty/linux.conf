# vim: ft=conf
# Included by kitty.conf via ${KITTY_OS}.conf.

# Geist Mono has no upstream italic — `auto` synthesizes one
font_family      GeistMono NFM
bold_font        GeistMono NFM Medium
italic_font      auto
bold_italic_font auto

font_size 12.0
